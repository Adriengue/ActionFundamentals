name: GitHub Action Demo
on:
  push:
    branches:
      - main
    paths:
      - '!ActionFundamentals/.github'
  pull_request:
    branches:
      - main
  schedule:
    - cron: 15 6 * * 0
  workflow_dispatch:
    
    
